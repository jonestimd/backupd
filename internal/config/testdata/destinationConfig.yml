backends:
  Google Drive:
    type: googleDrive
    config:
      clientConfig: gd_client_secret.json
sources:
- path: /home/me/Documents
  backend: # ignored
    type: other
  destination:
    backend: Google Drive
    folder: Backups/me
